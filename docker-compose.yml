services:
  # conversation-db:
  #   image: pgvector/pgvector:pg16
  #   environment:
  #     POSTGRES_USER: metis
  #     POSTGRES_PASSWORD: ${CONVERSATION_DB_PASSWORD:-metis}
  #     POSTGRES_DB: metis
  #   ports:
  #     - "127.0.0.1:5434:5432"
  #   volumes:
  #     - conversation_pgdata:/var/lib/postgresql/data

  backend:
    build: .
    ports:
      - "127.0.0.1:8001:8000"
      # - conversation-db
    env_file:
      - .env
    volumes:
      - ./metis:/app/metis       # ← código live
    command: ["uvicorn", "metis.main:app", "--host", "0.0.0.0", "--port", "8000", "--loop", "asyncio", "--reload"]

volumes:
  conversation_pgdata:
